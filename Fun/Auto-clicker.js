javascript:clicker: {    const {        Number    } = self;    const milliseconds = Number.parseInt(self.prompt('How many milliseconds split clicks? (1 second is 1000 ms)', '0'));    if (false === Number.isSafeInteger(milliseconds)) {        self.alert('Input was not an integer');        break clicker;    }    let clientX = 0,        clientY = 0;    const {        document    } = self;autoclicker = self.setInterval(() => {        document.elementFromPoint(clientX, clientY)?.click?.();    }, milliseconds);document.addEventListener('mousemove', event => {        ({            clientX,            clientY        } = event);    }, {        passive: true    });var listener = (e) => {   if (e.key.toLowerCase() == "escape") {if (confirm('Are you sure you want to stop the autoclicker?')) {clearInterval(autoclicker);self.alert("Stopped autoclicker.");self.removeEventListener("keydown", listener);return;}}};self.addEventListener("keydown", listener);self.alert('Started autoclicker! Press ESC to stop.');}
